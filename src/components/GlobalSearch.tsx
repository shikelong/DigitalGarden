import React from "react";

interface IGlobalSearchProps {}

const GlobalSearch = (props: IGlobalSearchProps) => {
  return <div className="inline-block mr-2">Search</div>;
};

export default GlobalSearch;
